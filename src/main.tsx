import React from 'react';
import ReactDOM from 'react-dom/client';
import NovoRise from '../novorise_job_board';
import './index.css';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <NovoRise />
  </React.StrictMode>
);
